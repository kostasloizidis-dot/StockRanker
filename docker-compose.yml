version: '3.9'
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.Api
    ports:
      - "5139:80"
    environment:
      - ASPNETCORE_URLS=http://+:80
  ui:
    build:
      context: .
      dockerfile: Dockerfile.Ui
    ports:
      - "5169:80"
    environment:
      - ASPNETCORE_URLS=http://+:80
      - ApiBaseUrl=http://api:80
    depends_on:
      - api
